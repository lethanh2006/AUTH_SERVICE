export enum AppRole {
  ADMIN = 'admin',
  MANAGER = 'manager',
  CHEF = 'chef',
  CASHIER = 'cashier',
  WAITER = 'waiter',
  USER = 'user',
  VIP = 'vip',
}

export const APP_ROLES = Object.values(AppRole);
