name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: Shared Node.js quality gate
    uses: lethanh2006/Logger/.github/workflows/reusable-node-ci.yml@d274b289a561fd7c6e3637995482a75a89af4773
    with:
      platform-ref: d274b289a561fd7c6e3637995482a75a89af4773
